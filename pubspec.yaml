name: nrb_example_template
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  nrb: ^3.0.1
  permission_handler: ^12.0.1
  device_info_plus: ^12.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
